#add in ~/.bashrc next lines:
#if [ -f ~/.bash_zaz ]; then
#    . ~/.bash_zaz
#fi





# ====colors for scripst============
COLORSNUM=$(tput colors)
if [ "$COLORSNUM" -eq 256 ]; then
    #==========256 colors =======
    RED=$(tput setaf 160)
    GREEN=$(tput setaf 112)
    BLUE=$(tput setaf 38)
    YELLOW=$(tput setaf 220)
    ORANGE=$(tput setaf 208)
    NC=$(tput sgr0)
    REV=$(tput rev)
else
    #==8 colors ================
    RED=$(tput setaf 1)
    GREEN=$(tput setaf 2)
    BLUE=$(tput setaf 6)
    YELLOW=$(tput setaf 3)
    ORANGE=$(tput setaf 3)
    NC=$(tput sgr0)
    REV=$(tput rev)
fi

#check colors
#for i in $(seq 0 $(tput colors) ) ; do tput setaf $i ; echo -n " [█${i}] " ; done ; tput setaf 15 ; echo
#===========================


#================CUSTOM_FUNCTIONS===================
__currentTTY()
{
#echo `tty | sed -e "s:/dev/::"`
temp=$(tty) ; echo ${temp:5}
}

__printor()
{
    TERMCOLS=$(tput cols)

    MSG=${1}
    MSGLNGTH=${#MSG}
    STARTLINE=$(printf "%-$((${TERMCOLS}/2 - ${MSGLNGTH}/2))s" "*")
    STLNLNGTH=${#STARTLINE}
    ENDLINE=$(printf "%-$((${TERMCOLS} - (${STLNLNGTH} + ${MSGLNGTH})))s" "*")

    echo $ORANGE"${STARTLINE// /*}"$GREEN${MSG}$ORANGE"${ENDLINE// /*}"$NC
}

__printfulline()
{
    #TERMCOLS=$(tput cols)
    #FULLLINE=$(printf "%-${TERMCOLS}s" "*")
    #echo $ORANGE"${FULLLINE// /*}"$NC
    __printor
}


#====================ALIASES======================
alias userls='cut -f1 -d: /etc/passwd'
alias vm='startVM.sh'
alias s='cd ..'
alias cds='cd ~/_WORK'
alias tt='tabbed -c xterm -into'
alias cdn='cd `getNexusPath.sh`'


PATH=$PATH:~/_bin


export HISTCONTROL="ignoredups"



